import type { Components } from '@mui/material/styles';

import type { Theme } from '../types';

export const MuiFormGroup = { styleOverrides: { root: { gap: '1rem' } } } satisfies Components<Theme>['MuiFormGroup'];
